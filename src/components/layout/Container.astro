---
import type { HTMLAttributes } from 'astro/types';
import clsx from "clsx";

interface Props extends HTMLAttributes<"div"> {}

const className = Astro.props.class;
---

<div class={clsx("max-w-[1200px] mx-auto", className)}>
  <slot />
</div>
