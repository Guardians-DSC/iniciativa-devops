---
import Container from './Container.astro';

interface Props {
  title: string;
}

const { title } = Astro.props as Props;
---

<section class="mb-8">
  <Container class="px-4">
    <h2 class="font-bold text-3xl mb-8">{title}</h2>
    <slot />
  </Container>
</section>
